<%= form_for(@assignment_question) do |f| %>
  <% if @assignment_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment_question.errors.count, "error") %> prohibited this assignment_question from being saved:</h2>

      <ul>
      <% @assignment_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Assignment_ID %><br />
    <%= f.number_field :Assignment_ID %>
  </div>
  <div class="field">
    <%= f.label :Question_ID %><br />
    <%= f.number_field :Question_ID %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
